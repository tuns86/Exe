module.exports = function () {
  // Chỉ cần load config VietQR (không cần khởi tạo SDK gì hết)
  const vietqrConfig = require("../config/vietqr.config");

  // console.log("✅ VietQR config loaded:", vietqrConfig.bank);
};
