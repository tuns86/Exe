<div id="body" class="container-wrap">
  <div class="row top-section">
    <!-- Carousel -->
    <div class="col-sm-8">
      <div id="slideshow" class="carousel slide shadow-lg rounded-lg overflow-hidden" data-ride="carousel">
        <ol class="carousel-indicators">
          <li data-target="#slideshow" data-slide-to="0" class="active"></li>
          <li data-target="#slideshow" data-slide-to="1"></li>
          <li data-target="#slideshow" data-slide-to="2"></li>
        </ol>
        <div class="carousel-inner">
          <div class="carousel-item active">
            <a href="/course/<%= proCourses[0].name %>">
              <img src="<%= proCourses[0].poster %>" class="d-block w-100 img rounded" />
            </a>
          </div>
        </div>

        <a class="carousel-control-prev" href="#slideshow" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon"></span>
        </a>
        <a class="carousel-control-next" href="#slideshow" role="button" data-slide="next">
          <span class="carousel-control-next-icon"></span>
        </a>
      </div>
    </div>

    <!-- Banners -->
    <div class="col-sm-4">
      <a href="/course/<%= proCourses[1].name %>" class="banner">
        <img src="<%= proCourses[1].poster %>" class="rounded shadow-sm hover-zoom" />
      </a>
      <a href="/course/<%= latestCourses[1].name %>" class="banner">
        <img src="<%= latestCourses[1].poster %>" class="rounded shadow-sm hover-zoom" />
      </a>
    </div>
  </div>

  <!-- Courses Sections -->
  <div class="most-care-courses section">
    <h4 class="heading-xl">3 khóa học được quan tâm nhất</h4>
    <%- include('../partials/card-course', {courses: proCourses, type: 'proCourse'}) %>
  </div>

  <div class="most-view-courses section">
    <h4 class="heading-xl">10 khóa học được xem nhiều nhất</h4>
    <%- include('../partials/card-course', {courses: mostViewCourses, type: 'mostViewCourse'}) %>
  </div>

  <div class="most-newest-courses section">
    <h4 class="heading-xl">10 khóa học mới nhất</h4>
    <%- include('../partials/card-course', {courses: latestCourses, type: 'latestCourse'}) %>
  </div>
  <!-- <hr /> -->
  <!-- <div class="most-view-category">
    <h2>5 lĩnh vực được đăng ký nhiều nhất</h2>
    <% for( let i = 0; i < mostSignUpCategory.length; i++ ) { %> <% if(i==0 ||
    i==3 ){ %>
    <div class="row">
      <% } %> <% if( i==3 ){ %>
      <div class="row second">
        <div class="col-sm-2"></div>
        <% } %>
        <div class="col-sm-4">
          <a href="/courses/<%= mostSignUpCategory[i].name %>"
            ><button class="btn btn-outline-warning">
              <div class="icon">
                <% if(i==0){ %>
                <i class="fa fa-laptop"></i>
                <% } %> <% if(i == 1 ){ %>
                <i class="fas fa-chart-line"></i>
                <% } %> <% if(i == 2){ %>
                <i class="fas fa-ad"></i>
                <% } %> <% if(i==3){ %>
                <i class="fa fa-music"></i>
                <% } %> <% if(i==4){ %>
                <i class="fas fa-pen-fancy"></i>
                <% } %>
              </div>
              <%= mostSignUpCategory[i].name %>
            </button></a
          >
        </div>
        <% if(i==2 ){ %>
      </div>
      <% } %> <% if(i==4 ){ %>
    </div>
    <% } %> <% } %>
  </div> -->
</div>

<style>
  /* General Layout */
  .container-wrap {
    width: 85%;
    margin: auto;
    padding-top: 48px;
  }
  .row.top-section {
    margin-top: 20px;
  }

  /* Carousel */
  .img {
    height: 350px;
    object-fit: cover;
  }
  .carousel-indicators li {
    margin: 0 5px;
    height: 12px;
    width: 12px;
    border-radius: 50%;
    background-color: rgba(37, 99, 235, 0.4) !important;
  }
  .carousel-indicators .active {
    background-color: #2563eb !important;
  }
  .carousel-control-prev,
  .carousel-control-next {
    display: none; /* hide arrows for cleaner look */
  }

  /* Banner */
  .banner img {
    height: 170px;
    margin-bottom: 12px;
    width: 100%;
    object-fit: cover;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    border-radius: 10px;
  }
  .hover-zoom:hover {
    transform: scale(1.03);
    box-shadow: 0 6px 18px rgba(0,0,0,0.15);
  }

  /* Section Headings */
  .heading-xl {
    font-family: "SF Pro Display", -apple-system, BlinkMacSystemFont, Roboto, sans-serif;
    font-weight: 700;
    font-size: 24px;
    padding-bottom: 8px;
    margin-bottom: 20px;
    border-bottom: 2px solid #2563eb;
    color: #1e3a8a;
  }

  .section {
    margin-bottom: 40px;
  }

  /* Courses */
  .course {
    margin: 0 10px 20px 10px;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .course:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 16px rgba(0,0,0,0.15);
  }
  .course .image {
    width: 100%;
    margin: auto auto 10px auto;
    border-radius: 10px;
    overflow: hidden;
  }

  /* Footer */
  .footer-container {
    padding: 30px 0 20px 0;
    font-size: 16px;
    background: #1e3a8a;
    color: white;
    text-align: center;
  }
</style>
<!-- <style type="text/css">
  .course-carousel {
    margin-right: auto;
    margin-left: auto;
  }

  .course-more-detail {
    display: none;
  }

  .course:hover .course-more-detail {
    display: block;
  }
  .navbar-collapse {
    flex-grow: 0;
  }

  .navbar-collapse-right {
    margin-left: 750px;
  }

  .container-wrap {
    width: 85%;
    margin-left: auto;
    margin-right: auto;
    padding-top: 48px;
  }

  .img {
    height: 350px;
  }

  .carousel-control-prev {
    left: 250px;
  }

  .carousel-control-next {
    right: 250px;
  }

  .udlite-container {
    width: 100%;
    max-width: 1340px;
    margin-right: auto;
    margin-left: auto;
    padding: 30px 2.4px 30px 2.4px;
  }

  .heading-xl {
    font-family: sf pro display, -apple-system, BlinkMacSystemFont, Roboto,
      segoe ui, Helvetica, Arial, sans-serif, apple color emoji, segoe ui emoji,
      segoe ui symbol;
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: -0.02px;
    font-size: 24px;
    padding-bottom: 10px;
  }

  .grid-container {
    display: grid;
    grid-auto-columns: max-content;
    grid-auto-flow: column;
    grid-gap: 16px;
  }

  .banner img {
    height: 170px;
    margin-bottom: 10px;
  }

  .carousel-control-prev {
    display: none;
  }

  .carousel-control-next {
    display: none;
  }

  .carousel-indicators li {
    border: 0px;
    margin: 0 5px;
    height: 15px;
    width: 15px;
    border-radius: 20px;
  }

  .row.top-section {
    margin-top: 20px;
  }

  .course .image {
    margin-right: auto;
    margin-left: auto;
    margin-bottom: 10px;
    width: 100%;
  }

  .course {
    margin: 0 10px;
  }

  .heading-xl h2 {
    margin-left: 12px;
    padding-bottom: 10px;
    margin-bottom: 10px;
    border-bottom: 1px dotted #2563eb;
  }

  .section-feature {
    margin-bottom: 30px;
  }

  .section-feature i {
    margin-right: 10px;
    float: left;
    font-size: 40px;
    color: #3b82f6;
  }

  .section-feature h2 {
    color: #3b82f6;
  }

  .section-feature .col-sm-4 {
    padding: 20px;
  }

  .section-feature .col-sm-4 .block {
    text-align: center;
    padding: 20px;
    border-radius: 5px;
    border: 4px dotted #3b82f6;
  }

  .footer-container {
    padding-top: 30px;
    padding-bottom: 20px;
    font-size: 12px;
    text-align: center;
  }
  .footer-container {
    font-size: 20px;
    background: #1e3a8a;
    color: white;
  }
  .most-view-category .row {
    margin-bottom: 20px;
  }
  .most-view-category .row button {
    width: 100%;
    min-height: 250px;
  }
  .most-view-category .second button {
    width: 100% !important;
    min-height: 250px !important;
  }
  .second .btn-outline-warning {
    width: 100% !important;
    min-height: 250px !important;
  }
  .most-view-category .row .center {
    margin: 0 auto;
  }
  .most-view-category h2 {
    margin-bottom: 20px;
  }
  .btn-outline-warning i {
    font-size: 60px;
  }
  .btn-outline-warning {
    font-size: 20px;
  }
  .btn-outline-warning .icon {
    margin-bottom: 10px;
    font-size: 60px;
  }
  .section-feature label {
    font-size: 25px;
    font-weight: 700;
  }

  .btn-outline-warning {
    color: #2563eb !important;
    border-color: #2563eb !important;
    background-color: transparent !important;
  }

  .btn-outline-warning:hover {
    color: white !important;
    background-color: #2563eb !important;
    border-color: #2563eb !important;
  }

  .btn-outline-warning:focus,
  .btn-outline-warning.focus {
    box-shadow: 0 0 0 0.2rem rgba(37, 99, 235, 0.25) !important;
  }

  .btn-outline-warning:not(:disabled):not(.disabled):active,
  .btn-outline-warning:not(:disabled):not(.disabled).active {
    color: white !important;
    background-color: #1d4ed8 !important;
    border-color: #1d4ed8 !important;
  }

  .carousel-indicators .active {
    background-color: #2563eb !important;
  }

  .carousel-indicators li {
    background-color: rgba(37, 99, 235, 0.5) !important;
  }
</style> -->
<script type="text/javascript">
  $(".owl-carousel").owlCarousel({
    items: 4,
    loop: true,
    margin: 4,
  });
</script>
